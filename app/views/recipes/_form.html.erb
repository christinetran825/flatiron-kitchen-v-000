<h1>Add a new recipe</h1>

<%= form_for recipe do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %>
  <br>
  Choose existing ingredients:
  <br>
  <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %>
  <br>

  <%= f.submit %>
<% end %>
